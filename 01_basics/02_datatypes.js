"use strict"; //treats all js code as per newer version.

// alert(6+6) // As we are nodejs and not the browser it gives error

// Code reabaability should be high....

// DATATYPES:-

// number => 2^53
let num = 33

// bigint => for larger numeric values 

// string => "  "
let name = "xyz"

// boolean => true / false
let flag = true

// null => standalone value
let goal = null

// undefined => value is not assigned
let count;

// symbol => used to store unique values

// object

console.log(typeof undefined) // Undefined datatype

// console.log(typeof(goal))
console.log(typeof null) // Object datatype